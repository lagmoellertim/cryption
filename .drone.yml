kind: pipeline
name: default

steps:
- name: build
  image: node
  commands:
    - npm install
    - npm build
- name: publish  
  image: plugins/gh-pages
  settings:
    username:
      from_secret: github_username
    password:
      from_secret: github_password
    pages_directory: build/